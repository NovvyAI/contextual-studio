# 修改历史

本文档记录 Contextual Studio 的功能新增、行为变化、问题修复、配置调整和项目文档更新。记录按时间倒序排列。

## 未发布

### 修复：工作台"正在处理"提示语滞后于实际阶段

- `describeWorkingTask()` 原先把最近 8 条消息拼成一段文本、按固定优先级顺序做关键词匹配，导致处于多步连续确认链（例如确认视听方向后自动生成分镜）时，容易命中一条更早阶段遗留在窗口内的旧消息，显示落后于实际进度的提示（如已经在生成分镜候选，却仍显示"正在生成落版方向候选"）。
- 现在优先信任 `workspace.productionPlan` 里的结构化状态字段；没有对应字段时，改为从最新一条消息开始逐条往回匹配关键词，命中即停止，确保最新一条"现在自动生成…"消息始终优先于更早的旧消息。

### 修复：ImaRouter 参考图稳定化引入的回归

- 稳定化对象路径不再把远程 URL 的签名 token 等查询字符串计入哈希，避免同一张图片因供应商临时签名轮换而反复重新下载上传、在 GCS 里堆积重复对象。
- 缓存命中检查与实际上传目标改为共用同一个不依赖 Content-Type 的对象路径，消除两者算出不同扩展名导致缓存永远不命中的问题。
- 图片校验改为直接嗅探文件头（PNG/JPEG/WEBP/GIF），不再仅凭远程 Content-Type 判断；避免部分对象存储用 `application/octet-stream` 等通用类型返回真实图片时被误判为非图片而中止整个视频生成。
- 使用新模型重试逐镜视频生成时，清理残留 `generating` 任务不再局限于当前请求的模型；避免其他模型下遗留的孤儿任务在服务重启恢复时被误当作"被中断的任务"，用过期模型重新触发一次未经用户确认的付费生成。

### 修复：ImaRouter 远程参考图 404 与失败镜头假运行

- 提交 ImaRouter 前将外部人物图和分镜图下载并转存到 `IMAROUTER_GCS_BUCKET` 的稳定、确定性地址，上传后验证公网可读，避免 Novvy 临时路径被截断或失效导致 `wget 404`。
- 某个镜头准备素材或生成失败时，现在会把对应逐镜任务明确标记为失败并保存原始错误；恢复生成时跳过已经完成的镜头，只为失败或未开始的镜头创建新任务。

### 修复：确认六视图后分镜误报人物参考组为空

- 修复后续 Novvy 结构化对话剥离已确认参考组中的原始人物 URL，且分镜生成器无法解析“图片编号｜URL”详情文本的问题；参考组 URL 现在纳入 workspace 合约并在每轮确定性保留。
- 已受影响的工作台会从最近一次“确认人物身份参考”记录恢复原始人物图并回补 workspace；历史记录不足时再从确认卡详情提取内嵌 URL，无需重新生成人物或六视图。

### 调整：短剧分析超时可配置

- 每集 Codex 剧情分析超时由固定 15 分钟改为读取 `.env` 的 `DRAMA_ANALYSIS_TIMEOUT_MINUTES`，默认 60 分钟；无效值回退到默认值，并限制最大 1440 分钟。

### 新增：工作台统一错误诊断卡

- 工作台中的用户操作校验、Novvy/ImaRouter、授权、网络、安全审核、数据库权限和未知异常现在统一生成聊天诊断卡，保留原始错误并说明来源、通俗原因、是否可解决、处理建议及自动重试策略。
- 同步接口错误会在原页面提示之外写入聊天记录；后台异步任务的 `error_message` 会在刷新工作台时自动补充诊断且按原始错误去重。未知错误明确标记证据不足，不虚构根因；重试仍使用原任务卡的上下文入口，避免重复任务或重复计费。

### 修复：窄幅人物裁切图无法生成六视图

- 人物参考图上传前新增确定性尺寸标准化：宽度不足 300px 的 YuNet 裁切图会保持宽高比放大至 300px，再进入 Novvy 上传与六视图生成；不裁切人物，已合规图片不改变尺寸。

### 新增：单视频与多集文件夹短剧分析

- 短剧入口支持上传单个视频或包含多集视频的文件夹，按文件名自然排序后逐集保存和分析。
- 每集改为只分析前 900 秒并每 5 秒抽取一帧，每 20 帧生成一张连续概览拼图；同一集的全部拼图一次送入 Codex SDK，替代固定均匀 20 帧。
- 逐集截图和 YuNet 人物候选记录新增集号归属；整剧视图合并逐集时间线、人物、情绪、台词、母题与创意衔接，同时保留逐集结果和内容指纹缓存。

### 修复：超出镜头上限时提前预警

- 分镜方案超过生产 Profile 的镜头上限时，选择方案后立即显示预警并禁用统一确认按钮；后端统一确认入口也会再次拒绝超限方案，避免直到创建视频时才出现“最多 3 镜”。

### 修复：六视图生成前的人物参考上传

- 人物参考上传与图片生成统一优先使用项目 `.env` 中的 `NOVVY_MCP_URL` 和 `NOVVY_MCP_AUTHORIZATION`，避免已安装插件的旧配置导致只有六视图上传链路失败。
- 上传脚本失败时解析并展示结构化原因和修复提示，不再只向用户显示缺少根因的整条 `Command failed` 命令。

### 修复：已有视频截图上传资产不再占用 Codex 会话

- “截图 XX 上传为资产”现在由确定性后端直接把已保存的短剧截图登记到资产区，不再启动 Codex 对话；重复提交会返回已有资产，避免长时间占用同一 thread 后触发 `already has an active writer`。
- “截图 XX 上传为人物参考图”同样改为确定性登记：直接生成可勾选、可修改的人物候选卡，并按用户给出的角色称呼归组，不再送入普通 Codex 对话等待文字回复。
- 左侧当前人物阶段把“人物参考图选择”移动到节点标题之后，新登记候选无需越过剧游策略和长卡片内容即可立即看到并勾选。
- 左侧“人物、道具与参考图”详情区不再只显示最后一条消息中的人物卡，而是汇总当前工作台全部有效人物候选；新增女主截图后，原人物和新增人物会同时保留展示。
- 人物参考详情卡改为双列并排，并在标题显示当前人物候选总数，避免第一张长图把后续候选推到下方而被误认为没有加入。
- 资产区域的“人物参考图”分类现在合并短剧检测候选、用户从截图登记的人物候选以及后续生成或修改的人物图；人物候选不会只进入普通图片资产而漏出人物参考分类。
- 本地页面的 HTML、CSS 和 JavaScript 响应禁用浏览器缓存，刷新工作台即可获得最新人物候选布局和交互代码，避免数据已有两张但旧页面仍只渲染一张。
- 从异常状态重试截图登记成功后，会恢复到原人物候选审核阶段并清除底层执行错误；聊天区只显示面向用户的资产结果，不再暴露 Codex thread-store 堆栈。

### 修复：动态人物库不再由人脸检测数量决定

- 短剧分析的动态人物库现在必须覆盖剧情分析识别出的所有可区分角色，不再预设男主、女主或固定槽位；主角、共同主角、配角、反派、非人及蒙面角色都可独立进入人物候选。
- 每个剧情角色新增 1-3 个概览帧兜底：YuNet 有高质量结果时同时使用人脸候选，没有检出人脸时仍用20帧剧情截图展示身份、服装或完整人物形象。
- YuNet 候选扫描适度放宽检测置信度、最小脸尺寸和质量阈值；旧分析缓存只有部分人物人脸归组时，重新分析会自动重建完整角色库，而不会继续复用不完整结果。

### 修复：部分用户视频任务误报缺少英文提示词

- 修复新版 `contextual.video-shot-plan.v1` 转换时忽略镜头 JSON 内嵌英文提示词、且过度要求卡片详情标签精确结尾的问题；现在按 `promptEn`、兼容别名和带镜头 ID 的详情字段依次回退，避免任务在调用视频模型前被错误拦截。

### 文档：记录逐镜人物参考过滤待办

- 暂缓上线“每个视频镜头只发送实际出镜人物六视图”的契约改动；实施方案、暂缓原因、兼容要求和验收标准已记录到 `TODO.md`，当前视频生成继续使用全部已确认人物参考。

### 调整：ImaRouter MiniMax 改用 H3

- 最终视频模型列表移除 MiniMax Hailuo 2.3，替换为 ImaRouter `MiniMax-H3`；请求使用官方 `/v1/videos` 契约、`768P`、`9:16`、4–15 秒整数时长和 `role_mode=reference`。
- H3 每镜按人物六视图面板在前、对应分镜图在后的顺序提交最多 9 张公网参考图，不再使用 Hailuo 的单一 `metadata.first_frame_image` 首帧契约。

### 修复：单个视频镜头重生成误入人物候选

- 视频镜头修改候选卡现在使用确定性的单镜重生成接口，直接找回该镜头原视频任务、提示词和已确认参考素材，只创建被点名镜头的新版本；不再把卡片全文送回通用聊天意图识别，因此不会因“人物参考图”等文字误跳到人物候选阶段。
- 单镜重生成可以重新选择 Novvy MCP 或 ImaRouter 具体模型；其他已完成镜头、人物图、分镜图和落版图均保持不变。

### 新增：ImaRouter 视频模型可选

- 最终逐镜视频生成阶段现在可以在 ImaRouter 下选择 Seedance 2.0 Fast、Kling v3 Omni、MiniMax Hailuo 2.3 或 Vidu Q3 Turbo；模型随逐镜任务持久化，服务重启后仍按原选择恢复。
- MiniMax 按其首帧生视频契约，仅提交该镜头对应的已确认分镜图作为 `first_frame_image`，不会把人物六视图误当首帧；其他模型继续使用人物身份面板在前、对应分镜图在后的参考顺序。

### 修复：新版逐镜视频任务契约无法执行

- 修复正式视频提示词使用 `contextual.video-shot-plan.v1` 和 `shot-A01` 等来源镜头 ID 时，被旧执行器误报“最多 3 镜”的问题；执行前会确定性转换为内部连续镜头 ID，并从同卡中绑定对应的中英文逐镜提示词，同时为版本、空镜头和超限分别返回准确错误。

### 调整：处理中显示实际任务

- 聊天框顶部不再只显示笼统的“任务处理中”，会根据生成中卡片、生产计划和最近的确定性流程消息显示当前实际工作，例如人物六视图、落版方向、视听方向、文字分镜、逐镜分镜图、真实落版图、正式视频提示词或逐镜视频，并附带当前用时。
- 修复服务恰好在正式视频提示词生成期间重启时错误回退到人物参考确认的问题；落版图已确认且尚无视频提示词卡时会自动恢复该步骤并保留全部前置成果。

### 修复：落版生成稿丢失英文提示词

- 修复落版方向确认时保存了同 ID 的精简卡，导致末镜校准后的生成稿缺少 GPT-image-2 英文提示词的问题；新确认会优先采用字段最完整的候选，已有工作台点击生成时也会从历史原始卡自动回补并保留末镜约束。

### 调整：默认使用 ImaRouter 生成视频

- 视频提示词确认卡默认选中 ImaRouter，并明确显示“六视图人物面板 + 分镜图”；用户仍可手动切换到 Novvy MCP，后端在未传供应商时也统一回退到 ImaRouter。

### 修复：方案字母分镜标签无法生成图片

- 修复模型输出 `shot-A01`、`shot-B02` 等“方案字母 + 镜头号”标签时，分镜解析器误报“这套分镜没有可生成的镜头内容”的问题；现有文字分镜无需重写即可直接生成逐镜图片。

### 调整：ImaRouter 六视图与分镜参考顺序

- ImaRouter 逐镜提交统一改为每个出镜人物的独立六视图面板依次在前、该镜头对应分镜图固定在最后；英文提示词明确人物面板只约束身份，最后一张分镜图负责构图与动作，并禁止把六视图网格复现在成片中。
- 真人隐私检测降级重试同步适配新顺序，能够正确判断被移除的是人物面板还是末位分镜图；Novvy MCP 继续使用分开的 `humanImageUrls` 与 `imageUrls`，不依赖数组位置混合语义。
- 人物确认改为两段式：第一次锁定人物身份后，系统基于该人物全部已选原图自动补齐六个固定角度并生成 2×3 面板候选；用户可在原卡修改，且必须为每个人物确认一张面板后才会进入落版方向。
- 已确认参考组同时保存原始人物图与六视图面板：静态分镜和 Novvy MCP 继续使用原始人物参考，ImaRouter 只使用六视图面板；服务重启只恢复未完成的面板，不重复生成已完成角色。
- 本地创意与分镜 Skill 同步补充六视图固定槽位、单人物单面板和人工审核门槛。

### 新增：人物参考图字幕与水印清理辅助

- 短剧人物候选的本地 OpenCV YuNet 扫描新增保守的字幕、标题和角落水印区域检测；候选保存覆盖区域、覆盖评分和待清理状态，人物质量排序会降低文字覆盖帧的优先级，从相邻采样候选中优先选择同角度的干净画面。
- 分析阶段不会自动擦除原图或批量调用付费模型；主页对仍含疑似文字的正式候选给出提示，工作台人物参考图卡新增“一键标注字幕/水印”，把本地检测区域转换为现有 PNG alpha 蒙版，用户审核修改意见后才提交局部清理。
- 原始短剧截图永久保留；清理结果继续以新“图片 XX”资产保存，并同步右侧同一人物候选。检测只用于辅助选择和蒙版起点，不对被遮挡的人脸或服装作无审核推断。

### 修复：服务重启后逐镜视频恢复错误回退

- 修复逐镜视频已有部分镜头成功、另一镜因服务重启中断后，工作台错误回退到选择导演和重写剧情的问题；启动恢复现在识别最近活动的 Novvy MCP 或 ImaRouter 逐镜批次，保留并跳过已完成镜头，只恢复查询或提交真正未完成的镜头。
- 恢复逻辑覆盖“任务已有 taskId，轮询被中断”和“素材准备或首次提交时尚未写入 taskId”两种窗口；全部镜头已经完成但最终批次状态尚未来得及写入时，只补齐逐镜复审状态，不重复生成。
- 新版启动时也会修复已被旧版本错误降级的现有工作台：同一视频方案只要同时存在成功镜头和中断的生成中镜头，就直接恢复该批次；没有任何成功镜头的陈旧孤立任务不会被自动唤醒。

### 修复：多套分镜图缺少方案选择

- 修复同时生成两套分镜图时，右侧把六张图片合并提交并报“请选择同一套方案的分镜图”的问题；“统一确认”区域现在按所属方案提供单选卡，展示每套名称、完成张数和内部方案 ID，只提交用户选择的完整一套，不再混合不同方案镜头。

### 真实游戏玩法视频分析设计

- 新增 `docs/GAMEPLAY_VIDEO_ANALYSIS_DESIGN.md`，定义在游戏分析阶段上传真实玩法视频、提取带时间证据的操作与 UI 约束，并将玩法真实性与短剧视听风格分别编译进后续分镜和视频提示词的产品方案；当前只包含设计，不改变运行代码。
- 进一步明确最高生成原则：“玩法真实性是硬约束，短剧画风是视觉约束”；设计中补充两类约束的职责边界、提示词强制分区、玩法还原 UI 文案和生成前硬门槛，禁止为了短剧表现改写真实玩法，也禁止游戏原视频画风覆盖短剧视觉世界。

### 人物参考图支持标注修改与重绘

- 资产区域的每张“人物图 XX”现在都提供修改意见框、“标注图片位置”和“按意见重新生成”；点选、框选、箭头及各区域说明会与总体修改意见一起提交。
- 新增确定性人物参考图重绘路由：始终以用户选择的原视频人物截图作为第一张高保真参考图调用图片生成，其他编号资产仅作为显式补充参考。
- 重绘结果作为新的“图片 XX”进入生成图片区域并保留原始人物截图，也不会让已经推进的工作流退回人物审核阶段。
- 修复从左侧“人物图 XX”重绘只产生资产、右侧原人物候选仍显示旧图的问题：后端现在通过人物编号与候选编号组成的稳定 card id 精确匹配右侧卡片，不依赖两套显示序号；提交时原卡显示生成中，完成后同步更新其真实预览与版本。无法匹配历史卡时才创建同一人物与角度的新候选，避免误改其他人物。
- 已有工作台中由旧临时 card id 生成成功的图片可以直接迁移回对应人物候选，复用现有真实图片 URL，不重复提交付费图片任务。

### 图片局部编辑与引用修复

- 人物图、分镜图、道具/参考图及资产区域的“标注图片位置”统一升级为真实 PNG alpha 蒙版编辑；后端会把 JPEG、WebP 或本地上传原图按需转换为与蒙版同尺寸的 PNG，再提交给图片模型。
- 落版图支持真正的 PNG alpha 局部编辑蒙版：前端按原图尺寸生成透明编辑区，后端校验并上传蒙版，再通过 Novvy MCP 的 `maskUrl` 传给 GPT Image；修改位置不再依赖文字坐标。

### 右侧聊天补充自定义创意方案入口

- 修复创意方案审核阶段只有左侧画布提供“自定义创意方案”、右侧聊天A-D候选组缺少入口的问题；未选择方案时，右侧候选组末尾现在同步显示想法输入框和“生成自定义方案 E/F…”按钮。
- 自定义入口仍沿用同一确定性流程：只新增一张候选，保留A-D和其他历史方案，不自动选择方案或推进人物阶段；任务处理中会随其他交互一起禁用。

### 资产区域自动收录短剧人物参考图

- 工作台资产接口新增动态人物参考图集合，从短剧人物库中为每个角色选择正面、左右3/4和左右侧面的最高质量人物形象图；无需等到创意确认或人物生成阶段才出现。
- 资产区域新增独立“人物参考图”分区和稳定的“人物图 01、02…”编号，展示人物名称、角度、原视频时间和质量分；点击可查看完整人物形象，也可一键写入聊天输入框。
- 聊天和图片生成修改框现在可直接引用“人物图 02”等编号，后端会解析真实本地候选并按需上传为生成参考，不改变既有“图片”和“截图”编号。
- 调整资产分区顺序，恢复“生成图片”置顶；原有图片资产、上传为资产和根据描述生成图片入口始终首先可见，人物参考图放在其后，避免新增人物卡把原功能推到页面下方造成“消失”的误解。

### 短剧分析加入本地动态人物候选扫描

- 新增 OpenCV YuNet 本地全片扫描：按约每秒 2.5 帧检测人脸，以五点关键点判断正面、左右侧面和左右 3/4 角度，并按脸部大小、清晰度和亮度筛选候选；原始视频帧不发送给第三方人脸服务。
- 短剧分析结果新增动态 `characterLibrary`，允许任意数量的主角、共同主角、配角和反派，不再假定每部剧都有一男一女两位主人公；Codex 仅根据候选拼图保守归组匿名人物并标注剧情身份。
- 数据库新增人物候选表和只读图片接口，保存候选编号、人物编号、时间戳、角度、质量分及截图；短剧详情接口同时返回动态人物候选。
- 人物参考图审核优先展示动态人物库中每个角色各角度的最佳候选，并继续保留旧四槽位兼容回退，避免已有工作台中断。
- 首页短剧分析详情和工作台短剧展开页新增动态人物候选展示，可查看人物归组、角度、时间戳和完整候选截图。
- 修复已有动态人物库时主页仍把旧男主/女主正侧面四槽位作为正式区域展示的问题；新分析只展示动态人物候选，四槽位仅作为旧工作台后端兼容投影，旧分析才显示“历史主人公参考截图”。
- 修复短剧分析器仍选择系统 Python 3.14、导致已安装的项目 OpenCV/YuNet 未运行且动态人物卡只有文字没有图片的问题；分析器现在明确优先使用 `.venv/bin/python`。
- 动态人物缓存只有人物名称但所有 `candidateIds` 为空时不再视为完整结果；重新分析会使用本地人脸候选拼图再次归组并回填真实人物图片。
- 动态人物卡默认直接展示该人物每个可用角度中质量最高的真实脸部截图，并标明完整候选数量，避免只有角色文字，也避免一次铺满数十张近似连续帧。
- 人物扫描改为双层截图：紧凑脸部图只供检测与匿名归组，页面和后续人物参考使用从原帧向下扩展的人物形象图，尽可能保留发型、帽子、服装、首饰、体态和伤情；原镜头没有拍到的身体部分不会生成或补画。
- 环境安装固定使用 Python 3.12，自动安装 OpenCV 和本地 YuNet 模型；已安装且版本正确的依赖与模型会自动跳过。MediaPipe 在 Intel macOS 无窗口后台进程中存在 OpenGL 初始化失败，因此未作为生产硬依赖。
- 修复全新环境脚本在系统已经有合格 Node.js 时仍强制通过 Homebrew重复安装的问题；Node 24、Python 3.12、FFmpeg现在逐项检测并跳过，避免旧款 Intel Mac 因无可用 Homebrew bottle 提前中止。

### 修复末镜校准后的落版图提示词识别

- 落版图生成后端现在同时识别“后续图片生成提示词”“图片生成提示词”以及旧版 GPT-image-2/英文提示词字段，避免已存在完整提示词却显示“缺少 GPT-image-2 英文提示词”。
- 新增字段兼容回归测试；现有工作台无需重新生成落版方向或分镜，刷新后可以直接重试真实落版图生成。

### 视听方向卡固定显示视听语言 Bible

- 视听方向的内容契约本身仍是完整 Bible；聊天卡标题改为确定性显示“视听语言 Bible V1”，不再依赖模型是否在标题中写出 Bible。
- 已生成的旧视听方向卡会在右侧聊天卡、左侧当前节点和已确认成果节点统一显示“视听语言 Bible”，卡内构图、机位、运镜、表演、光色、剪辑、声音、连续性和末镜衔接内容保持不变。

### 落版方向移至人物确认后并用末镜校准真实落版图

- 工作流调整为“确认创意 → 人物与参考图 → 落版方向 → 视听方向 → 剧情与分镜”；确认创意后不再提前生成落版候选或真实图片。
- 落版方向只确认末镜衔接、9:16 构图、英文文案、视觉层级、色彩和产品真实性边界；确认方向后才进入视听方向与分镜设计，末镜必须落实该方向。
- 逐镜分镜图统一确认后，系统以末镜分镜图作为连续性参考准备真实落版图；用户点击生成并审核通过后，才自动生成正式视频提示词。
- 同步更新服务重启恢复逻辑和项目本地 Skill，避免旧流程在恢复时跳回“创意后先落版图”。

### 导演风格改为完整本地库下拉选择

- “视听方向”不再由 AI 推荐导演或只展示 2–3 个候选；页面改为下拉框直接列出本地视听质量库中的全部 60 位唯一导演，并保留“不使用导演参考”选项。
- 导演列表和风格参数由后端确定性读取项目本地矩阵；用户确认时后端重新校验导演名称，并把库内已有的构图、运镜、光色、剪辑与声音等可观察参数写入确认成果，客户端不能伪造风格说明。
- Novvy 只负责生成当前项目的视听语言 Bible，不再推荐、选择或列举导演；后续分镜使用用户明确选择的导演参数，且禁止复制具体作品或镜头。

### 创建工作台后主页自动刷新

- 点击“进入创意工作台”并成功创建后，新标签页继续立即打开对应工作台，原主页同时自动刷新历史工作台列表并恢复创建按钮；用户返回主页即可看到新记录，无需手动刷新页面。

### 自定义创意方案候选

- 创意方案 A/B/C/D 之后新增“自定义创意方案”输入卡；用户可以直接描述自己的剧情连接、玩法、情绪、节奏或文案方向，由 Novvy 补齐为一张完整可审核的候选方案。
- 自定义方案从 E 开始递增编号并保留历史候选；生成时后端强制保留 A-D、其他自定义方案、已确认成果和制作状态，不会因为生成自定义方案而自动选择、进入人物阶段或提前生成落版图。
- 生成后的自定义方案支持与原候选相同的修改、重新生成和选择采用操作；已有方案确认后输入卡自动隐藏，避免在下游制作阶段误新增上游候选。

### 创意方案长文本卡片内换行

- 创意方案的剧尾依据、尾帧状态、最后对白和其他详情支持在卡片可用宽度内自动换行；中英文混排、长英文对白及连续字符也能强制安全断行，不再横向溢出画布或聊天候选卡。

### 旧版短剧分析自动迁移

- 服务启动时自动把已完成的 `version: 2` 与 `novvy.video-analysis.v2` 短剧记录转换为 `novvy.video-analysis.v3` 规范节点，使既有短剧无需重新上传或再次调用模型即可回到主页创意来源列表。
- 迁移保留旧分析根字段用于审计，把剧情、人物、情绪、对白、母题、创意交接、置信度和人物参考截图 ID 确定性映射到 `episodeAnalyses[].detailedAnalysis` 与 v3 参考槽位；缺失字段明确使用空值或低置信度，不虚构新事实。
- 迁移在单个数据库事务中完成；格式损坏或无法识别的记录保持原样，不影响其他有效短剧和服务启动。

### README 补充 Telemetry 完整说明

- README 新增 telemetry 工作原理、事件类型、双层 SQLite 存储、隐私边界、本地启动顺序、监控页功能、状态检查、失败重试、旧工作台回补和切换公司远端服务说明。
- 项目结构补充本地 telemetry 数据库、dashboard 文件、采集服务、outbox、MLflow tracing 及对应启动脚本。

### 本地 Telemetry 监控网页

- 本地采集服务新增 `/dashboard` 监控页，展示采集总量、任务列表、阶段时间线、公开资产和用户反馈，并支持搜索、标签切换、手动刷新和每 10 秒自动刷新。
- 新增只读的本机 dashboard 数据接口；输出会隐藏认证字段、本机绝对路径及 URL 查询参数，不展示完整聊天、附件内容或模型推理。
- telemetry 写入 API 继续要求 `X-API-Key`，监控页仅由监听 `127.0.0.1` 的本地服务提供，不应暴露到公网。

### 创意方案标注剧情依据或剧尾依据

- A/B/C/D 四个首轮创意方案新增明确的“创意依据”与“依据证据”，区分方案由剧中具体剧情点还是剧尾生成；四个候选至少同时覆盖一种剧情点方案和一种剧尾方案。
- 剧尾方案必须展示尾帧状态、最后对白、残留情绪和未完成钩子；剧情点方案必须指出具体情节、人物行动或关系变化，禁止只写笼统题材匹配。
- 右侧创意候选卡和左侧画布卡片标题区新增“剧情点/剧尾”醒目标记，完整内容中展示依据字段；修改方案上下文和历史方案兼容展示也同步加入，旧方案显示“历史方案未标注”，不会因字段升级无法打开。

### 创意内容时长改为可变

- 移除第一轮创意方案默认或暗示“12 秒内容”的规则；每个方案改为根据剧情承接、真实玩法操作、可见反馈和升级钩子所需信息量，给出建议内容时长范围及节奏依据。
- 正式内容时长由确认后的逐镜 `durationSeconds` 相加并扣除镜间交叉淡化得到；继续遵守单镜 4–15 秒、最多 3 镜，3 秒落版单独追加且不占内容时长。
- 修复单视频成片路径会把模型视频固定截为 12 秒再追加 3 秒落版的问题；现在完整保留本次请求的内容视频时长，再追加已确认落版图。
- 视频完成卡不再显示“前12秒”，改为展示本次实际内容镜头时长和单独追加的落版时长。

### 落版图修改提示词精简

- 修复落版图修改意见引用“图片 XX”或“截图 XX”时只把编号写入提示词、却没有把对应真实图片传给图片模型的问题；当前落版图固定作为第一张输入，编号参考图按反馈中的顺序追加。
- 修改已生成落版图时不再回灌首轮完整设计提示词，只发送当前图片、用户修改意见和必要的不变项约束，减少旧设计描述与最新反馈冲突。

### 文档

- 新增 `CODEBASE_OVERVIEW.md`，系统梳理当前代码架构、核心数据流、状态机、数据库、API、外部依赖、运行边界与维护风险。

### 创意工作台 v3 来源加载修复

- 创意工作台历史列表只加载绑定 `novvy.video-analysis.v3` 短剧的 Session，保留但不再序列化会触发严格 v3 校验失败的旧版 Session。
- 短剧/游戏来源与历史工作台改为独立容错加载，历史记录异常不再阻断新分析短剧进入选择下拉框。

### 短剧分析输出契约去重

- 新生成的短剧分析升级为 `novvy.video-analysis.v3`，详细剧情只保存在 `episodeAnalyses[].detailedAnalysis`，不再把时间线、人物、情绪、对白、母题和创意交接复制到根节点；单视频也不再机械生成重复的 `seriesAnalysis`。
- 前端展示、创意上下文、人物参考图、素材登记和采集只读取 v3 的 `episodeAnalyses[]`；不保留 v2 根级字段回退，旧记录需重新分析后才能继续使用。
- 人物参考角度改为受限枚举，并按参考槽位确定正侧面，修复自然语言角度导致 `female_side` 被投影成 `front`；同时修复视听母题前后端字段名不一致造成的空白展示。

### MLflow 本地运行时

- 新增独立分支上的 MLflow 3.15.2 Server/UI、本地 SQLite/Artifact 启动脚本及官方 `@mlflow/core` TypeScript tracing SDK，为后续 Codex SDK、Novvy MCP 和 ImaRouter 全链路埋点提供运行基础。
- 一键安装脚本会在项目 `.venv` 中按 `requirements-mlflow.txt` 安装 MLflow；`npm run mlflow:server` 或 `./start_mlflow.sh` 在回环地址 5050 启动 UI，避开 macOS Control Center 常用的 5000 端口，数据保存在被 Git 忽略的 `data/mlflow/`。
- 新增统一脱敏 tracing 边界，覆盖 Codex SDK `thread.run()`、Novvy MCP `callTool()` 和 ImaRouter HTTP 请求；记录编译后 Prompt、最终输出、工具参数、结果、耗时与异常，并通过 session metadata 关联工作台。
- 发送 trace 前移除认证字段、签名查询参数、base64、二进制和本机绝对路径；新增 `GET /api/mlflow/status` 检查运行时是否实际启用。

### 分镜稳定编号解析兼容

- 分镜图生成器除“镜头 01”“Shot 01”外，新增识别 `A-01｜…`、`B-02｜…`、`storyboard-C-03｜…` 等方案前缀稳定镜头编号。
- 修复视听方向阶段生成的新式文字分镜明明包含三个镜头，却提示“这套分镜没有可生成的镜头内容”的问题。

### 分镜图修改改为逐卡锁定

- 修改某一张分镜图时只锁定该卡片；同组其他已生成分镜仍可填写反馈、标注画面并提交独立的重新生成任务。
- 后端允许同一组不同分镜图并行修改，但拒绝对正在生成的同一卡片重复提交；所有分镜任务结束前继续禁止统一确认。
- 多张分镜并行修改时，会话保持处理中，最后一张任务完成后才恢复到剧情与分镜审核阶段。
- 其他分镜卡片中正在输入的反馈以页面内草稿保存；任一生成任务完成触发卡片刷新时，不会清空尚未提交的文字。

### 聊天附件确定性登记为资产

- 聊天框上传图片并输入“上传为资产”“保存为资产”或“加入资产区域”时，由后端直接把本地原图登记到资产区域并分配“图片 XX”编号。
- 左侧资产区域末尾的“添加图片”卡改成上下两部分：上方支持一次选择最多 6 张本地图片并直接登记为资产；下方保留文字描述、引用现有资产并调用图片模型生成新图的原有能力。
- 该路径不调用 Codex、Novvy MCP 或图片生成模型，不依赖平台域名解析，也不会改变当前创作阶段。
- 本地上传资产后续参与图片或视频生成时，才通过现有上传脚本按需转换为云端可访问 URL。

### 剧情与分镜前增加视听方向确认

- 人物参考图组确认后不再直接生成剧情与分镜，先由同一 Novvy 创意会话生成一张「视听方向」卡片。
- 卡片展示可执行的视听语言 Bible，包括构图、机位与焦段、运镜触发、光色、表演、剪辑、声音、连续性和禁止项。
- 导演风格参考改为 UI 可选项：展示 AI 推荐、2–3 个替换候选以及“不使用导演参考”；每项显示转译后的可观察参数。
- 用户确认视听方向后才自动进入剧情与分镜，确认结果作为左侧工作流节点长期保存并供后续分镜约束使用。

### 剧情与分镜描述自动分段

- 剧情与分镜、逐镜分镜图片的长描述在左侧画布、右侧候选卡和已确认成果中按“剧情功能、开始、峰值、结果、反应、摄影、声音、参考绑定”等语义标签自动换行，并保留模型原有换行。
- 该处理仅作用于页面展示，不修改数据库中的原始分镜文字，也不改变后续图片或视频生成使用的提示词。

### 首次剧游创意等待体验与输入精简

- 首次进入工作台时不再把短剧分析中的单集、整剧和顶层重复副本全部发送给 Codex；改为只保留剧情时间线、人物关系、情绪、对白、视听母题、片尾钩子、参考图、结尾连续性及完整游戏产品分析，降低首次剧游匹配的输入体积。
- “正在生成剧游连接与创意方向”改为动态进度状态：显示已等待时间，并依次提示读取分析、建立连接、生成候选和质量检查；页面明确说明轮询仍在运行、无需重复点击，减少正常长任务被误判为掉线。

### 创意工作台左右面板可调宽度

- 画布与聊天框之间新增可拖动分隔条，桌面端可按当前任务自定义两侧宽度；拖动结果保存在浏览器本地，刷新和再次进入工作台后继续沿用。
- 支持双击分隔条或按 Home 恢复默认 2:1 比例，也支持聚焦后用左右方向键微调；宽度带有画布与聊天框最低限制，850px 以下继续使用原有上下布局。

### 首次视频分析缓存契约修复

- 修复当前模型输出 `narrativeContinuity`、而视频分析缓存仍强制要求旧字段 `seriesContinuity`，导致模型已分析成功却在写缓存阶段被标记失败的问题；缓存校验以新字段为主并继续读取旧字段。
- 缓存读取会把旧版 `seriesContinuity` 规范化为当前下游使用的 `narrativeContinuity`；缓存写入今后即使因契约升级失败，也不会再拖垮主任务，分析结果与截图仍会保存到项目数据库，避免用户盲目重试并重复消耗模型分析。
- 对已经发生该故障的记录，重试时会从旧错误中恢复 `--analysis-json` 后的完整模型结果并重新落库，不再重复调用模型；仅在旧错误没有保留完整 JSON 时才重新分析。

### 分镜方案镜头结构兼容

- 修复其他电脑的 Codex 返回“第 1 镜”“镜头一”、嵌入式镜头段落或结构化 `shots` 数组时，页面有分镜方案但后端误报“这套分镜没有可生成的镜头内容”的问题。
- 分镜图生成器现在统一提取多种中英文镜头格式并规范化内容，仍最多采用 3 镜，且不会把整体剧情、受众或风险说明误识别为镜头。

### 创意助手 Codex 会话工作区写权限

- 创意工作台主 Codex 会话由 `read-only` 改为 `workspace-write`，允许在用户明确要求时写入项目目录和项目 SQLite 数据库，避免操作因 `attempt to write a readonly database` 中断。
- 权限仍限定在项目工作区，保持 `approvalPolicy: never` 与网络关闭；短剧分析、游戏分析和视频辅助线程继续只读，不扩大无关流程的权限。

### 分镜图跨电脑 Python 与失败重试修复

- 修复新电脑可能直接调用系统 Python 3.9，导致上传脚本在 `int | None` 类型注解处、远程请求前崩溃的问题；本地图片上传现在强制通过项目 Skill resolver 选择 Python 3.10+，优先使用 `CONTEXTUAL_PYTHON` 或项目 `.venv`。
- 上传脚本启用延迟类型注解，避免旧解释器仅在导入 `UploadError` 时产生难以理解的 `unsupported operand type(s) for |`；找不到合格 Python 时改为明确提示重新运行环境安装脚本。
- 分镜图组存在缺图或失败卡时，统一确认继续禁用并显示“重试失败的分镜图”按钮；重试直接走后端，只处理失败镜头并保留成功图片，不再要求用户在只读 Novvy 对话中输入“重试上传”。

### 新电脑自动安装 Novvy 插件

- `install_environment.sh` 默认安装 Codex CLI，从 `NovvyAI/skills` 浅克隆或更新 `novvy-ad-creative`，再执行插件自己的幂等安装脚本；不依赖开发者电脑上的绝对路径。
- 新电脑尚未配置 Novvy 权限时，交互式安装会用隐藏输入读取本机 `admin_user.apikey`，写入被 Git 忽略且权限为 `600` 的 `novvy-plugin-local.json`，由插件安装器同步到运行缓存 `.mcp.json`；非交互安装或直接回车时安全跳过并引导运行 `$novvy-env-check`。
- 新增 `--without-novvy-plugin` 选项；已有插件仓库、Python 环境、依赖和本机 Key 均会复用或跳过，不覆盖现有私有配置。

### 本地创意采集服务

- 新增可独立启动的本地 telemetry 接收端，兼容 Skill 版本解析、run、stage、asset、feedback 和完成状态接口；公司远端未部署时，可把 Contextual Studio outbox 发送到 `127.0.0.1:4191`。
- 本地接收数据独立保存到 `data/local-telemetry.sqlite`，支持 `X-API-Key` 校验、阶段/资产/反馈幂等去重、健康检查和接收数量状态接口；新增 `start_local_telemetry.sh` 与 `npm run telemetry:local`。
- 本地服务只监听回环地址，不应暴露到公网；README 和环境变量示例补充本地启动及监控方式。

### ImaRouter 真人隐私自动降级重试

- 逐镜视频首次提交仍使用对应分镜图和已采用人物参考图；若 ImaRouter 明确返回 `InputImageSensitiveContentDetected.PrivacyInformation` 或 `may contain real person`，会根据 `content[n]` 定位实际输入图片、移除该图，并仅自动重试一次。
- 自动重试会在对话中说明移除了哪张分镜图或人物参考图，并在成功后的逐镜卡片记录降级策略；其他错误和第二次失败不会无限重试。
- 若某一镜明确触发输出音频版权限制，仅把该镜通过 `metadata.audio=false` 降级为无音频并自动重试一次；已完成镜头和其他镜头不重做，成功卡片会标明该镜为无音频。

### 逐镜视频启动修复

- 修复已确认分镜图以“图片07：https://...”形式保存时，Novvy 和 ImaRouter 只接受 URL 开头字符串而误判为 0 张分镜图的问题；两条视频路径现在都会从带编号说明的字段中提取真实 URL。
- “确认并生成逐镜视频”请求失败时新增明确弹窗，不再把后端错误藏在卡片修改输入框的校验提示里。

### Novvy 创意过程采集

- 选择性融合 `novvy-creative-record` 的 run、stage、asset、feedback 与完成状态契约，新增项目本地 `.agents/skills/novvy-creative-record`；不覆盖 Contextual Studio 已定制的创意、分镜、逐镜视频和双供应商流程。
- 新增 SQLite `creative_telemetry_runs` 与可靠 `creative_telemetry_outbox`：事件先本地落库，使用幂等键去重，远端失败后指数退避，采集不可用不阻塞业务。
- 工作台创建时记录短剧与游戏分析摘要；创意助手阶段结果、落版图生成与确认、人物参考组确认、最终视频及用户审核反馈由后端确定性记录，不依赖 Codex 主动调用采集工具。
- 新增可选环境变量、`GET /api/telemetry/status` 和 `POST /api/telemetry/retry`；不配置远端时事件仅保留本地，不上传 API key、Cookie、base64、完整聊天、模型推理或本机绝对路径。
- 新增 `POST /api/telemetry/backfill/<工作台ID>`，可从旧工作台已保存的消息、候选卡、公开资产和确认状态生成稳定幂等回补事件，多次执行不会重复入队。

### 人物参考图上传环境修复

- 恢复 PNG/JPEG 在缺少 Pillow 时通过标准文件头读取尺寸的兼容路径，避免人物参考图尚未上传就阻断生成。
- 环境安装脚本新增项目级 `.venv` 和 Pillow 的幂等安装：已有虚拟环境或 Pillow 时自动跳过；人物上传优先使用该项目 Python，也可通过 `CONTEXTUAL_PYTHON` 显式覆盖。

### 落版方案自动衔接

- 左侧落版方案节点新增独立修改历史链：从聊天记录中按落版卡 ID 汇总初始版、用户修改意见、生成中状态和完成版本，不再只有创意方案能回看版本。
- 修复左侧工作流把仅有文字候选的 `final_card` 阶段错误显示为“落版图已生成”的问题；现在没有真实 `previewUrl` 时显示“落版图方案待确认”，只有最新落版卡确有图片时才显示“落版图已生成”。
- 服务启动时会识别被重启中断的落版方案文字候选任务，并在同一 Novvy 会话里自动续跑；不会因此提交付费图片任务。无法安全自动续跑的未知任务会退出“处理中”，恢复到可重试状态，避免页面永久卡住。
- 修复确认创意方案后偶发停在“落版图准备”的问题：确认按钮现在明确要求同轮返回 2–4 张 `final_card` 文字方案候选，而不是模糊地询问下一步。
- 后端增加确定性阶段门校验；若首次 Codex 输出已确认 concept 但漏掉落版候选，会在同一个 Codex session 自动补跑一次结构化请求，同时强制保留 `selectedConceptIds` 和 `confirmedCards`。第二次仍缺失时返回明确可重试错误，不再伪装为后台处理中。

### 图片标注修改

- README 新增点选、框选和箭头的适用场景、逐步操作、示例文案、多区域输入及工具选择原则，作为项目内长期使用说明。
- 候选卡、资产图片区和视频截图区的图片点击行为改为当前页面内的大图预览弹窗，不再新开浏览器标签；支持关闭按钮、点击弹窗外部和 Esc 退出。
- 人物图、分镜图、落版图、参考图和资产图片新增“标注图片位置”，支持点选、框选、箭头、撤销和清空；标注自动转换为区域编号及归一化百分比坐标，并随用户自然语言修改意见一起进入原有 Novvy 图片重绘流程。
- 每个标注区域在弹窗内提供独立修改要求输入框，用户可直接填写“区域 1 删除文字、区域 2 更换背景”等要求；未填写区域说明时不会提交，避免坐标与文字意图错配。
- 增强箭头标注可见性：加粗高亮箭身、放大带深色描边的箭头头部，并显示清晰起点圆点，方便在复杂或浅色图片背景上辨认方向。
- 标注层不改写原图、不提前创建新资产；生成模型仍以当前真实图片为主参考，并被明确要求保留未点名区域。

### Contextual Ad Skill Suite 选择性吸收

- 将 Suite 的 `DramaUnderstandingPackage` 字段合同加入本地短剧分析 Skill，作为持久化整剧/跨阶段证据包的可选完整形态；保留当前网页分析使用的紧凑 `novvy.video-analysis.v2` 投影，不为满足大 Schema 虚构缺失事实。
- 将 `EmotionContinuityContract`、`AudiovisualLanguageBible` 和 `VisualDirectionBible` 加入本地视听语言 Skill，覆盖人物/观众情绪、品牌温度、节奏、画面、导演、剪辑、声音、特效、环境锚点和逐镜方向。
- 将 `ReferenceManifest`、`DialoguePerformanceManifest`、`CreativeProductionDossierManifest` 和 `VisualHandoffPackage` 加入本地分镜合同，用于参考用途与权限、说话者/对象/why-now/表演、自动生成审核全案和批准帧血缘；远程素材不得冒充本地文件哈希。
- 新增项目化图片预飞合同，要求逐镜覆盖接入、实体操作、实体结果、UI反馈、人物反应、更大钩子和高风险转场，并检查人物、空间、物件、摄影、表演、玩法、UI/VFX 与精确文字边界。
- 创意与分镜提示词接入 `输入 → 实体动作 → 实体结果 → UI反馈 → 人物反应 → 更大钩子` 因果链；UI、旁白或庆祝不得替代真实世界结果，人物反应不得先于可见结果。
- 新增最终视频技术 QC：拼接后执行完整 FFmpeg 解码与 FFprobe 检查，验证 720×1280、有效时长和音轨，记录编码、像素格式、SHA-256，并生成 1fps 联络表与 JSON 报告；技术失败时不进入最终确认。
- 明确未吸收的冲突项：不启用 Suite 的强制单次视频、不禁止用户授权重做、不限制 Novvy 仅生成落版图、不强制所有远程素材自动下载到本地，也不启用互动分支媒体执行。

### 可配置生产 Profile

- 新增版本化的 `config/production-profile.json`，把外部 Suite 硬编码的 `single_final_video_pass`、提交预算、成片数、重试、镜头数、时长、供应商和拼接参数改为项目本地可配置策略。
- 当前默认改为 Contextual Studio 已验证的 `reviewed_storyboard_multishot`：最多 3 镜、每镜 4–15 秒且默认 8 秒、静态分镜先审、逐镜复审、允许用户明确授权后重生成、Novvy/ImaRouter 双供应商、最终一支拼接成片并追加 3 秒落版图。
- `max_ai_video_submissions=null` 表示不设置固定总提交数；`auto_retry_allowed=false` 禁止后台失败后擅自重试，`user_authorized_retry_allowed=true` 保留用户主动重做能力。
- 新增生产 Profile 加载与启动校验，创意提示词、分镜任务校验、供应商准入、转场时长、镜头数量和落版时长均读取同一配置文件。

### 安装体验

- 新增面向全新 macOS 电脑的 `install_environment.sh` 一键环境安装脚本：自动准备 Apple 命令行工具、Homebrew、Node.js 24、Python 3、FFmpeg/FFprobe、项目 npm 依赖和可选 Google Cloud CLI，并引导完成 Novvy（Codex）登录。
- 安装脚本在 `.env` 不存在时仅复制安全模板，绝不覆盖已有配置或自动写入团队密钥；支持 `--without-gcloud` 和 `--skip-login`。
- README 增加电脑小白可按顺序执行的一键安装、系统弹窗、密码输入、登录和启动说明。

### 外部 `novvy-ad-creative` 2026-08-28 同步

- 同步外部新版的完整剧集纯视觉证据契约：每集均匀 20 帧合成唯一概览拼图、每集一次视觉分析、逐集 JSON 持久化与一次整剧汇总；单文件上传视为一集或用户提供的全剧合辑，精确缓存命中时不重复调用模型。
- 短剧分析改为只把概览拼图作为模型视觉输入，不再读取、转写或提示音频，也不把 20 张独立帧重复提交给模型；20 张原始截图仍保存到数据库并用于人物候选和资产区。
- 新增 `audiovisual-creative-quality.md`、包含 129 条质量记录与 60 个唯一导演样本的只读质量矩阵，以及按模块、字段、记录和导演查询的 `query_audiovisual_quality.py`；运行时只取必要记录并转译为可观察制作参数。
- 同步新版剧情高相关广告、创意策略、创意制作、参考图审核、商品分析、视频分析、叙事台词、上传与系列记忆契约；项目继续在同一 Codex session 内把这些“subagent”契约实现为隔离的结构化阶段，不在 UI 创建多个 task。
- 同步混合参考图快照与实际本地预览要求：支持 `imageUrls` 与 `humanImageUrls` 同时存在，上传/分镜审核必须绑定真实预览，`product_icon` 不进入生成输入。
- 同步分镜合同默认单镜 8 秒、最多三镜、逐镜稳定 ID/版本/同一参考快照校验；保留本项目“先生成并统一审核静态分镜图”门槛，以及 Novvy MCP/ImaRouter 双供应商和逐镜重做能力。
- 内容镜头拼接由硬切改为默认 `0.35s` 画面交叉淡化与同步音频交叉淡化；最终落版图仍沿用本项目方案，在内容镜头完成后确定性追加 3 秒静态卡，不让视频模型重绘文字。
- 同步上传重试、错误分类、失败槽位恢复、分镜拼接校验、结果素材下载等脚本增强；移除外部开发机绝对路径，项目本地脚本兼容 `novvy-plugin-local.json`、`.mcp.json`、`NOVVY_API_KEY` 与 `NOVVY_MCP_AUTHORIZATION`。
- 保留项目定制游戏入口：优先使用用户选中的已分析游戏；只有没有分析记录或用户点击“从 Novvy 产品库选择其他游戏”时才读取 Novvy 产品库。保留真人落版图逻辑、资产区、聊天卡片与现有工作台状态机。
- 外部旧 `tail-ad-insertion.md` 以及项目旧 `*-phase.md` 暂不删除，只作为历史兼容参考；生产规则改用 `drama-related-ad.md` 和新版结构化契约，避免破坏既有会话或旧数据。

### 修复

- 修复聊天框上传图片并要求修改时只返回文字候选卡、没有真实图片的问题；“图片附件 + 编辑指令”现在直接进入 GPT-image-2 图生图流程，上传图作为高保真主参考，结果同时显示在聊天卡片和资产区域。没有真实预览 URL 的视觉卡也不再允许显示“已生成”。
- 修复尚未生成图片的落版图方案在提交修改意见时，因卡片上下文包含人物说明而被误判为“添加人物候选”的问题；落版方案文字修改现在锁定原 card id、只更新该候选及英文提示词，并继续停留在落版图审核。
- 修复已生成落版图的“按意见重新生成”误走普通创意对话并跳转到人物与参考图的问题；现在使用当前落版图作为高保真主参考生成新版本，完成后继续停留在落版图审核阶段，只有用户点击“确认使用”才进入人物流程。

### 新增

- 右侧创意聊天区支持像 ChatGPT 一样直接拖入本地图片或视频；拖入时显示覆盖式高亮提示，松开后进入现有附件预览队列，可继续填写文字、移除附件并统一发送，“图片或视频”选择按钮继续保留。
- 最终成片确认后新增“一键打包落地页”：自动读取当前游戏名称、商店地址与图标，将成片压缩到 5 MB 内，生成带播放、静音、3 秒后 CTA、Novvy 生命周期兼容和本地事件埋点的自包含 HTML5 页面，并输出可直接下载的 ZIP；打包记录持久化到 SQLite，刷新页面后仍可下载最近版本。
- 新增项目本地 `audiovisual-language-design`、`creative-quality-review` 与 `storyboard-production-contract` 三个 Skill，将外部视听语言资料中的构图、机位、运镜、调度、光色、剪辑、声音、129 条质量矩阵和分镜合同融入创意工作流；保留项目现有静态分镜审核、最多三镜独立生成/修改、Novvy MCP/ImaRouter 选择和成片拼接，外部单次视频策略仅作为显式严格 profile。
- 用户提交方案修改后，左侧修改历史立即出现带原始意见的“生成中”版本节点；Novvy 返回后该节点更新为正式版本，无需等待右侧完成才看到记录。
- 左侧每个创意方案卡增加可展开的版本修改链，按 V1、V2…展示用户每次修改意见、新方案标题、摘要和时间；历史版本不会被最新版覆盖。
- 工作台底部聊天框支持同时添加文字、图片和视频，每条消息最多 6 个附件；附件可在发送前预览和移除，发送后随聊天历史持久展示。
- 图片附件作为真实多模态视觉输入进入当前 Novvy 会话；视频附件在本地均匀抽取 6 张关键帧后进入同一会话，原视频保留播放入口。
- 在右侧创意助手顶部增加固定的“当前待处理”区域，显示当前工作流阶段和最新待确认内容。
- 增加“定位到任务”按钮，可滚动到对应聊天卡片并短暂高亮。
- 聊天自动刷新时保留用户的历史阅读位置，不再无条件滚动到底部。

### 外部视听语言资料吸收说明

#### 已吸收并接入

- 将外部资料拆分为三个职责清晰、可独立版本化的项目本地 Skill：视听语言设计、创意质量审核、分镜生产合同；运行时只依赖 `.agents/skills/` 内的项目副本。
- 吸收构图、景别、机位、镜头运动、人物调度、光线、色彩、剪辑、声音、表演、UI 时机和连续性要求，并将其改写为可执行的镜头字段，不以导演姓名代替具体制作参数。
- 将文字分镜候选扩展为包含画面构图、摄影机、调度、光色、表演、声音、连续性和产品展示要求的镜头合同；分镜图片提示词也会携带这些信息。
- 引入分阶段质量检查：素材依据、角色与时空连续性、游戏真实玩法、文字与 CTA 可读性作为硬门槛；创意力度和视听完成度作为排序指标，不互相混淆。
- 收录外部规范化质量矩阵作为只读审核参考：共 129 条质量规则、60 个唯一导演样本；保留 `source_raw` 与 `normalization_note`，便于追溯原始内容及修复过程。
- 吸收“导演风格必须翻译为 2–4 个可观察参数”的规则，只借鉴构图、运动、光色、节奏等可执行特征，不要求模型模仿具体导演。
- 保留并接入 Contextual Studio 已验证的生产路径：先生成和审核静态分镜图片、最多三个镜头分别生成与修改、可选 Novvy MCP 或 ImaRouter、镜头拼接后确定性追加落版图。

#### 存疑或暂未吸收

- 未把外部 `single_final_video_pass` 设为默认硬规则。它与当前“逐镜生成、逐镜修改、双供应商选择”的产品交互冲突，目前仅在用户明确选择严格单次生成模式时作为可选 profile。
- 未整包复制外部“质量总控”Skill。该目录不是自包含版本，引用的 `workflow-contracts.md`、`single-video-pass-policy.md`、多个 schema、校验脚本以及独立图片/视频 Skill 均缺失；贸然接入会造成不可运行的生产依赖。
- 未将 Excel 表格作为运行时数据源。表内存在指向 `/Users/duminglun/Downloads/REXTRIX/...` 的失效绝对路径；项目以本地 JSON 矩阵作为审核参考，数据库与工作台状态仍是业务事实来源。
- 未按资料文字宣称“65 位导演”。实际解析得到 60 个唯一导演记录，其中 45 个完整、15 个字段不完整，另有一条重复来源记录；没有凭空补齐缺失的 5 位或 15 条不完整记录。
- 未把规范化过程中补出的内容当作用户原始证据。矩阵共有 43 条修复记录，部分字段是按语义补齐的可执行约束，因此运行时必须保留并参考来源与修复说明。
- 未沿用资料中含义不准确的术语，例如把 Overcrank 写成“插帧效果”、把画质劣化描述成“降格拍摄”；项目本地合同采用校正后的可执行定义。
- 未接入任意层级的 ENTRY/A/B 分支媒体生产。当前产品仍是线性创意工作流，最多三镜；若未来支持多版本、多分支视频族，需要先设计新的数据合同和画布交互。
- 未将外部权利证明、文件哈希、全部云媒体本地持久化等规则直接升级为运行时硬阻断，因为对应 schema、脚本和完整本地资产链尚不存在；本次只接入了与当前流程可验证的素材依据、连续性、产品真实性和文字质量检查。
- 未采用外部“失败后禁止自动重试”的统一规则；当前仍保留用户触发的逐镜重试与供应商切换能力。

### 调整

- 所有用户可见页面、状态提示和错误提示中的 `Codex` 品牌名称统一改为 `Novvy`；底层仍使用原有 SDK，不改变运行方式。
- ImaRouter 视频模型从 `seedance-2.0` 调整为 `seedance-2.0-fast`。
- ImaRouter 生成中、完成和失败卡片统一显示 `Seedance 2.0 Fast`。

### 文档

- README 补充其他开发者首次运行时的 Novvy MCP 鉴权要求，以及落版图反复失败时的 401/403 排查方法。
- README 增加聊天图片/视频附件的处理方式、存储位置和 `chat-media.js` 结构说明。

### 修复

- Novvy 生成期间，左侧画布在轮询数据没有变化时不再重复重建；确有新状态时会恢复方案卡、修改历史、确认成果和人物选择区的展开/收起状态，避免点击后立即失效。
- 修复在创意方案卡填写意见并重新生成时，文本被人物生成正则误判并直接跳到“人物、道具与参考图”的问题；方案 A/B/C/D 修改现在只更新目标方案并强制停留在创意方案审核阶段。
- 落版图生成和重试现在按同一个卡片 ID 只显示最新状态，避免“上方仍在重试、下方已经失败”的重复卡片；失败后的主操作明确显示为“重新生成落版图”。
- Novvy MCP 返回 HTTP 失败时保留服务端错误详情，并在 401/403 时提示检查本机鉴权配置，便于其他开发者定位 `.env` 配置问题。
- Novvy 处理任务期间，用户一旦向上滚动聊天记录就会暂停自动贴底；轮询更新会保留正在查看的历史位置，只有主动滚回底部或发送新消息后才恢复跟随最新消息。
- “定位到任务”现在依据当前流程中需要用户确认才能继续的真实操作按钮进行定位，优先跳到“确认并生成落版图”“确认人物参考图”“统一确认这组分镜图”等步骤，不再跳到最近的普通消息。
- 修复在聊天输入框中直接要求修改“分镜02”等指定分镜时只返回文字说明卡并复用旧图的问题；此类请求现在会定位最新的对应分镜图并启动真实图片编辑任务。
- 分镜重绘任务提交后，卡片图片区域显示“生成中”，不再用旧图冒充新结果。

## 2026-08-27

### 工作台与交互

- 建立短剧、游戏和创意工作台三个主要入口。
- 创意工作台改为独立浏览器页面，左侧为大画布，右侧为持续对话。
- 左侧确认成果采用蛇形节点布局和虚线引导线，可展开查看历史内容。
- 候选创意卡支持独立填写修改意见、重新生成、选择和折叠。
- 确认一个创意方案后自动折叠其他候选，同时保留再次展开能力。
- 对话中的创意方案、落版图、人物图、分镜图和视频以并列卡片呈现。
- 调整工作台卡片、对话和输入框字号，提高可读性。
- 创意助手生成中提示改为自然的人类表达，避免显示内部“整理画布”等状态。

### 短剧与游戏分析

- 增加本地短剧上传、分析、历史列表和详情查看。
- 短剧分析改为 Codex SDK 读取20张独立高清帧，并保留完整详细分析和紧凑创意契约。
- 增加剧情时间线、人物关系、情绪曲线、关键台词、视听母题、钩子和悬念输出。
- 增加男女主人公正面与侧面截图候选。
- 增加游戏商店页分析、历史记录和完整结果展示。
- 工作台优先使用用户已分析并选中的游戏，仅在没有记录或用户明确要求时读取 Novvy 产品库。
- 增加“从 Novvy 产品库选择其他游戏”入口。

### 创意制作流程

- 创意对话、方案、剧情台词、文字分镜和视频提示词统一通过 Codex SDK 运行。
- 同一工作台沿用同一个 Codex session，不在 UI 中创建多个 Codex task。
- 集成项目本地 `.agents/skills/novvy-ad-creative` 工作流。
- 保留内部的静态分镜图先生成、先审核流程。
- 增加落版图生成、确认和历史版本保留。
- 增加人物参考图候选、多选、统一确认和自定义人物图入口。
- 人物图按意见重新生成时使用原候选图作为高保真参考。
- 增加3套视频剧情与分镜候选以及逐镜分镜图生成和确认。
- 每个视频镜头绑定对应分镜图和已确认人物参考图。
- 左侧已确认剧情与分镜卡片在图片下方展示镜头文字描述，不再展示图片 URL。

### 图片与资产区域

- 增加工作台资产区域，生成图片获得稳定的“图片 01、图片 02……”编号。
- 增加独立视频截图区，截图获得稳定编号并可在聊天和修改框中引用。
- 每张生成图片支持填写反馈并重新生成。
- 每张资产支持从当前工作台删除，编号不会重排。
- 资产区域末尾固定保留“添加图片”空白卡，支持纯文字或引用现有资产生成新图。
- 从左侧“添加图片”生成的内部记录不再同步到右侧聊天框。
- 图片生成期间清空旧预览，并在原图片位置显示“生成中”。
- 修复视觉卡详情含真实截图 URL、但 `previewUrl` 为空导致卡片不显示图片的问题。
- 修复人物修改请求只生成说明卡、没有真正调用图片编辑的问题。
- 为人物图片和资产图片增加服务重启后的自动恢复。

### 视频生成

- 支持用户选择 Novvy MCP 或 ImaRouter 生成视频。
- Novvy MCP 使用 `seedance-2.0-fast`，支持多人物参考和对应分镜图。
- ImaRouter 支持多人物参考、分镜图输入、GCS 素材上传和逐镜生成。
- 增加 Google Cloud 登录入口及认证错误提示。
- 逐镜视频完成后在聊天和左侧画布中展示，可单独播放复审。
- 增加逐镜确认、视频拼接和确定性追加已确认落版图。
- 修复 ImaRouter 云端任务已经完成但本地仍显示“生成中”的问题。
- 持久化 ImaRouter 任务 ID；服务重启后恢复查询原任务，避免重复创建和重复收费。

### 数据与运行

- 使用 SQLite 保存短剧、游戏、工作台、消息、卡片、截图和视频任务。
- 上传原视频保存到 `data/uploads/`，短剧截图以 BLOB 保存到数据库。
- 增加服务重启后的中断任务恢复逻辑。
- 增加中文 `README.md`，说明功能、架构、配置、数据存储和常见问题。
- 增加 `start_server.sh`，支持启动、开发模式、环境检查和帮助信息。
- README 增加 `npm install`、`npx codex login` 和首次 Clone 启动流程。

### Skills 与仓库

- 将运行时 Skills 固定到项目 `.agents/skills/`，外部 Skill 和插件缓存仅作为参考。
- 合并外部 `novvy-ad-creative` 的叙事台词、视频分析、商品分析、创意策略、参考图审核、分镜契约和生成脚本，同时保留项目定制流程。
- 创建 GitHub 仓库 `NovvyAI/contextual-studio`，默认分支为 `main`。
- 仓库调整为公开；指定的 NovvyAI 组织成员获得写权限。
- `.env`、数据库、上传视频、`node_modules` 和运行缓存均通过 `.gitignore` 排除。
